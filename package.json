{
  "name": "preact-navbar-poc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && rm -rf dist && vite build && pnpm cleanup",
    "cleanup": "rm -rf dist/server && mv dist/client/* dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.9.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.3.0",
    "preact-render-to-string": "^5.2.1",
    "typescript": "^4.6.4",
    "vite": "^3.0.0",
    "vite-plugin-ssr": "^0.4.19"
  }
}